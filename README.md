# managed-hosting-tools
Managed Hosting Tools and Config files

- `moodle/` contains Moodle-specific scripts, with installer names indicating `nginx` vs `apache`.
- `pb/` contains Pressbooks-specific scripts and helpers.


env - Qbnox Hacked .env file
